This vagrant setup is just used to build a Chef Server from scratch
Once it is built then it is exported to an ova file that can be used in VirtualBox
as a 'pet' i.e. retains state / Chef recipes etc.