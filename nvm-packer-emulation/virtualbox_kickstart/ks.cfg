install
cdrom
lang en_GB.UTF-8
keyboard uk
# Only mod this once all tests passing
#lang en_US.UTF-8
#keyboard us

network --bootproto=dhcp --onboot=on
rootpw vagrant
firewall --disabled
selinux --permissive
timezone UTC
unsupported_hardware
bootloader --location=mbr --append="net.ifnames=0 biosdevname=0"
text
skipx
zerombr
clearpart --all --initlabel
autopart
auth --enableshadow --passalgo=sha512 --kickstart
firstboot --disabled
reboot --eject

user --name=vagrant --plaintext --password vagrant --groups=wheel

#%packages --instLangs=en_GB.utf8 --nobase --ignoremissing --excludedocs
%packages --nobase --ignoremissing --excludedocs

# vagrant needs this to copy initial files via scp
openssh-clients
sudo
kernel-headers
kernel-devel
gcc
selinux-policy-devel
bzip2
-fprintd-pam
#-intltool

# Unnecessary firmware
-aic94xx-firmware
-alsa-firmware
-alsa-tools-firmware
-ivtv-firmware
-iwl100-firmware
-iwl105-firmware
-iwl135-firmware
-iwl1000-firmware
-iwl2000-firmware
-iwl2030-firmware
-iwl3160-firmware
-iwl3945-firmware
-iwl4965-firmware
-iwl5000-firmware
-iwl5150-firmware
-iwl6000-firmware
-iwl6000g2a-firmware
-iwl6000g2b-firmware
-iwl6050-firmware
-iwl7260-firmware
-iwl7265-firmware
-linux-firmware
-avahi
-bluez-utils
-dogtail
-kudzu
-ipw2100-firmware
-ipw2200-firmware
%end

%post
# disable unnecessary services
chkconfig acpid off
chkconfig auditd off
chkconfig blk-availability off
chkconfig bluetooth off
chkconfig certmonger off
chkconfig cpuspeed off
chkconfig cups off
chkconfig haldaemon off
chkconfig ip6tables off
chkconfig lvm2-monitor off
chkconfig messagebus off
chkconfig mdmonitor off
chkconfig rpcbind off
chkconfig rpcgssd off
chkconfig rpcidmapd off
chkconfig yum-updateonboot off

# sudo
echo "%vagrant ALL=(ALL) NOPASSWD: ALL" >> /etc/sudoers.d/vagrant
chmod 0440 /etc/sudoers.d/vagrant

# Disable IPv6 - RCrouch - leave this out for now
#sudo -s 'echo "# Disable IPv6 - performed during Kick Start by Packer" >> /etc/sysctl.conf'
#sudo -s 'echo "net.ipv6.conf.all.disable_ipv6 = 1" >> /etc/sysctl.conf'
#sudo -s 'echo "net.ipv6.conf.default.disable_ipv6 = 1" >> /etc/sysctl.conf'

# Since we disable consistent network naming, we need to make sure the eth0
# configuration file is in place so it will come up.
# Delete other network configuration first because RHEL/C7 networking will not
# restart successfully if there are configuration files for devices that do not
# exist.
#rm -f /etc/sysconfig/network-scripts/ifcfg-e*
#cat > /etc/sysconfig/network-scripts/ifcfg-eth0 << _EOF_
#NAME=eth0
#DEVICE=eth0
#TYPE=Ethernet
#PROXY_METHOD=none
#BROWSER_ONLY=no
#BOOTPROTO=dhcp
#DEFROUTE=yes
#IPV4_FAILURE_FATAL=no
#IPV6INIT=yes
#IPV6_AUTOCONF=yes
#IPV6_DEFROUTE=yes
#IPV6_FAILURE_FATAL=no
#IPV6_ADDR_GEN_MODE=stable-privacy
#ONBOOT=yes
#_EOF_

%end
